kind = "web"
previewPath = "/"
title = "CollegeCompass"
version = "1.0.0"
id = "artifacts/college-compass"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18982

[services.development]
run = "pnpm --filter @workspace/college-compass run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/college-compass", "run", "build" ]
publicDir = "artifacts/college-compass/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18982"
BASE_PATH = "/"
